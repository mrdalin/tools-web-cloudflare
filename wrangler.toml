name = "tools-web"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"

# Cloudflare Pages deployment notes:
# - Do not put account_id, database_id, JWT, email keys, or OAuth secrets here.
# - Bind the D1 database in Cloudflare Pages dashboard:
#   Settings -> Functions -> D1 database bindings
#   Variable name: DB
# - Put runtime secrets in Cloudflare Pages environment variables.
